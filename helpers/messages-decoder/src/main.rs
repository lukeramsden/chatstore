//! Decodes an Apple Messages chat.db snapshot to versioned JSONL on stdout.
//! Read-only. Must never open the live database with write intent.

fn main() {
    eprintln!("chatstore-messages-decoder: not implemented");
    std::process::exit(70);
}
